#include "VariantVector.h"


int main()
{
   
    VariantVector testVector;

    testVector.addElement(5.5f),
    testVector.addElement("+");
    testVector.addElement(5.5);
    testVector.addElement("*");
    testVector.addElement(5.5);
    testVector.addElement("-");
    testVector.addElement(5.5);
    testVector.addElement("/");
    testVector.addElement(2);
    
    testVector.printElements();

    char exitKey = 'b';

    while(exitKey != 'q')
    {
        std::count
    }

    return 0;

}